# Banco SQLite (arquivo local)
DATABASE_URL="file:./dev.db"

# Segredo para JWT (gere um aleatório em produção)
JWT_SECRET="seu-segredo-super-secreto-mude-em-producao"

# Resend - envio de email (opcional; sem key o link de convite vem na resposta da API)
RESEND_API_KEY="re_xxxx"
EMAIL_FROM="Nossa Agenda <onboarding@resend.dev>"

# URL base do app (para links no email)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
